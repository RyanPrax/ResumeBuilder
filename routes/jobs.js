// RESTful router for jobs CRUD plus GET/POST /api/jobs/:id/bullets and PUT/DELETE /api/job-bullets/:id.

import db from '../lib/db.js';
import { Router } from 'express';

const router = Router();

export default router;
