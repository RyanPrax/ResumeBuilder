GEMINI_API_KEY=
PORT=3000

# Path to Chrome or Chromium binary for server-side PDF generation.
# Leave blank on standard Linux/macOS — the app auto-detects common install locations.
# Required on ARM64/WSL2: set to your system Chromium path (see docs/special-instructions.md).
CHROMIUM_PATH=